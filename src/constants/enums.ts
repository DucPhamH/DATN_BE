export enum IngerdientTypes {
  not_accept,
  accept
}

export enum UserRoles {
  level_1,
  level_2,
  level_3
}

export enum UserStatus {
  banned,
  active
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}
